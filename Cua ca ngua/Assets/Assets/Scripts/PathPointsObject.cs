using UnityEngine;

public class PathPointsObject : PathPoint
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public PathPoint[] CommanPath;
    public PathPoint WinPath;


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
